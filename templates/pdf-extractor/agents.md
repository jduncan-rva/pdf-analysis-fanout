# Extractor Operating Instructions

- Refer to the `pdf-layout-extractor` skill for schema definitions and table formatting.
- Check `/workspace/staging/<doc_id>/` before processing to avoid duplicate chunking.
- Ensure all output files pass JSON validation against `references/schema.json`.
