# Auditor Operating Instructions

- Ensure all findings from `/workspace/output/*.json` are incorporated into the synthesis.
- Generate the final markdown matrix following `templates/reconciliation-matrix.md` in the `financial-reconciliation` skill.
- Attach complete footnote references matching the `citation-auditor` style guide.
